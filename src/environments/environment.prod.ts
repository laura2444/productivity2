// src/environments/environment.prod.ts
export const environment = {
  production: true,
  // URL de tu backend en producción
  // Cámbiala cuando despliegues el backend
  backendUrl: 'https://productivityback.onrender.com/api'
  
};